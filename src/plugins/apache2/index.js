import types from './types'
import * as resolvers from './resolvers'
import * as config from './config'

module.exports = {
  types,
  resolvers,
  config,
  query: {
    name: 'apache2',
    type: 'Apache2',
  }
}

